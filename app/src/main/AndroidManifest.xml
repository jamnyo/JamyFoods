<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jamyfoods">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JamyFoods">
        <activity android:name=".Supper"></activity>
        <activity android:name=".EveningDinner" />
        <activity android:name=".Lunch" />
        <activity android:name=".Breakfast" />
        <activity android:name=".FoodTypes" />
        <activity android:name=".Hotels" />
        <activity android:name=".Profile" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>